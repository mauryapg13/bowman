{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bowman/certificates/inclusion",
  "title": "Certificate: code/inclusion.py",
  "module": "code/inclusion.py",
  "stage": "MVP M2",
  "spec": [
    "project_spec.md §4.3"
  ],
  "signature": "gate(events: list[RawEvent]) -> list[Event]",
  "input": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "events"
    ],
    "properties": {
      "events": {
        "type": "array",
        "items": {
          "$ref": "types.schema.json#/$defs/RawEvent"
        }
      }
    }
  },
  "output": {
    "type": "array",
    "items": {
      "$ref": "types.schema.json#/$defs/Event"
    }
  },
  "guarantees": [
    "Table §4.3 applied exactly: settled/scheduled included; pending debit included; pending credit excluded; cancelled/failed excluded; unrealized or non_cash excluded; amount None excluded as amount_unknown.",
    "Every excluded event has a non-null exclusion_reason; every included one has null.",
    "Order preserved; one output per input.",
    "Dataset counts: 8 pending_credit, 22 cancelled, 21 failed, 10 non_cash_unrealized, 16 amount_unknown (MVP)."
  ],
  "forbidden": [
    "reading linked_event_id (that is links.py)",
    "reading messages"
  ]
}
